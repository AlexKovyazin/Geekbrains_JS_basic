// Чему будет равен x?

var a = 2;
var x = 1 + (a *= 2);   /*
                        5. Происходит операция сложения 1 и переменной а, 
                        но при этом во второй части выражения её значение переназначается 
                        путём умножения значения переменной на 2.
                        */
